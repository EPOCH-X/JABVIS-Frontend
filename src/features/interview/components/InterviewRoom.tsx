/**
 * ================================================================
 * [프론트엔드] 모의면접 - 면접실 컴포넌트
 * ================================================================
 * 기능 요약:
 *   실시간 모의면접을 진행하는 메인 인터페이스입니다.
 *   AI 면접관 아바타, 질문 표시, 답변 입력(음성/텍스트)을 포함합니다.
 * ================================================================
 */

"use client";

export default function InterviewRoom() {
  // TODO: WebSocket 연결 + 실시간 면접 UI
  return (
    <div className="border rounded-lg p-6 min-h-[500px]">
      <p className="text-gray-400">면접실 영역</p>
    </div>
  );
}
